
const Navbar = ({height}) => {
  const navbarStyle = {
    height: height,
    width: "100%",
    "background-color": "#00a884"
  }
  return(
    <div className="navbar" style={navbarStyle}>

    </div>
  )
}

export default Navbar